let inputWidth;
let inputHeight;
inputWidth = prompt("Enter the width");
inputHeight = prompt("Enter the Heght");
width = parseInt(inputWidth);
height = parseInt(inputHeight);
let area = width * height;
document.write("area = " + area);
